node_modules/
dist/
dev-dist/
.env
.env.local
server/subscriptions.json

# Vendored by scripts/vendor-ocr.mjs at build time.
public/tesseract/
*.local
.DS_Store
scanner-check/
public/fonts/
app-check/

# A personal score sheet used for local scanner testing — never committed.
public/real-sheet.jpg

# Scratch files from poking at the pipeline in a browser.
*.tmp.*

# Google OAuth client files. The secret in one of these is a real credential —
# unlike the publishable key, which is public by design — and it belongs in the
# Supabase dashboard and nowhere else.
client_secret*.json
*.credentials.json

# Real score sheets used to test the scanner. They carry a name and a QR code,
# so they are local-only — same rule as public/real-sheet.jpg.
sheet-fixtures/
